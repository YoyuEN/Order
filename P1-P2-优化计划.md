# 乐梵小灶点餐 · 后续优化计划（P1 / P2）

> 生成日期：2026-08-12
> 前置：P0 健壮性修复已完成（见文末「P0 变更回顾」），本文件仅覆盖**剩余未实施**的优化项。
> 严重级别：🔴 高（影响正确性/安全） · 🟡 中（体验/稳定性） · 🟢 低（整洁/性能）

---

## 一、P1 —— 待实施的功能性与健壮性修复

### 1.1 🔴 无鉴权与限流（R2）

- **问题**：`POST /api/orders`、`PUT /api/messages`、`DELETE /api/dishes/:id` 等写接口无任何鉴权。CORS 只限制浏览器来源，`curl` / 恶意脚本可直接调用，可被灌单、清空菜单、删除菜品。
- **位置**：
  - `server/index.js` 全部写路由（orders / messages / dishes / uploads / favorite）
- **建议方案**（按投入从小到大）：
  1. 轻量级方案：`ALLOWED_API_TOKENS` 环境变量 + 自定义中间件校验请求头 `X-Api-Token`（匹配 `pool`/CORS 同源的 `ALLOWED_ORIGINS` 读取模式）；前端在 `apiRequest` 统一附带。
  2. 加限流：`express-rate-limit`，对 `/api/orders`、`/api/messages` 写操作限流（如 30 次/分钟/IP），防刷。
  3. 进阶：给订单/留言接口加简单校验码（店内点餐场景通常无需完整登录体系）。
- **注意事项**：PWA 离线缓存与 Capacitor WebView 需保证 token 在 `localStorage` 持久化，启动时恢复。

### 1.2 🔴 订单菜品存在性校验（R7）

- **问题**：`createOrder` 中 `dish_id` 用标量子查询 `(SELECT id FROM dishes WHERE id = ?)`，若前端传入不存在的 `dishId`，会静默插入 `dish_id = NULL`（配合外键 `SET NULL` 语义），订单里出现「幽灵菜品」。
- **位置**：`server/db.js` `createOrder` 的 items 循环（约 L470 起）
- **建议方案**：INSERT 前批量校验：
  ```js
  const ids = order.items.map(i => i.dishId)
  const [rows] = await connection.execute(
    `SELECT id FROM dishes WHERE id IN (${ids.map(() => '?').join(',')})`, ids)
  const existing = new Set(rows.map(r => Number(r.id)))
  const missing = order.items.find(i => !existing.has(i.dishId))
  if (missing) { await connection.rollback(); return { error: `菜品不存在: ${missing.dishId}` } }
  ```
- **注意事项**：`orderSchema`（index.js）只校验了 dishId 为正整数；校验不通过时应返回 400 而非 500。删除菜品后残留的旧订单快照不受影响（items 已有 dish_name 快照）。

### 1.3 🟡 请求体过大未返回 413（R6）

- **问题**：`express.json({ limit: '32kb' })` 超限时抛 `entity.too.large`，当前错误处理器未捕获 → 落到 500「服务器暂时无法处理请求」；且 32kb 对 UTF-8 中文留言（500 字上限约 1.5KB）虽足够，但菜品批量编辑/长 note 组合可能逼近。
- **位置**：`server/index.js` 错误处理器（`app.use((error, ...)` 内，`entity.parse.failed` 分支之后）
- **建议方案**：在错误处理器增加分支：
  ```js
  if (error.type === 'entity.too.large') {
    response.status(413).json({ error: '请求内容过大' })
    return
  }
  ```
- **注意事项**：上传接口走 `multer`（multipart），不受此 limit 影响，无需改 15MB 限制。

### 1.4 🟡 上传图片无垃圾回收（R8）

- **问题**：`uploads/dishes/` 下文件只增不减——菜品被删、或替换新图后，旧图文件永久残留，占用磁盘。
- **位置**：`server/db.js` `deleteDish` / `updateDish`（image 变更时）
- **建议方案**：
  1. 删除菜品时，`fs.unlink` 其 `image_url` 指向的本地文件（仅当路径以 `/uploads/` 开头）。
  2. `updateDish` 更换图片时删除旧文件。
  3. 可选：启动时扫描 `uploads/dishes/`，删除不在任何菜品 `image_url` 中的孤儿文件（注意多实例部署时用 pm2 单实例，`ecosystem.config.cjs` 已是 `instances: 1`，安全）。
- **注意事项**：外链图片（`http(s)://`）不落盘，跳过；删除文件失败不应阻塞主流程（try/catch + 日志）。

### 1.5 🟡 前端丢弃服务端错误信息（R9）

- **问题**：`apiRequest` 中 `if (!response.ok) throw new Error('API request failed: ' + response.status)`，服务端返回的 `{ error: '…' }` 被丢弃，用户只能看到笼统的「操作失败」。
- **位置**：`main.js` `apiRequest`（L70-89）
- **建议方案**：
  ```js
  if (!response.ok) {
    let message = `请求失败（${response.status}）`
    try {
      const body = await response.json()
      if (body?.error) message = body.error
    } catch { /* 非 JSON 响应 */ }
    throw new Error(message)
  }
  ```
- **注意事项**：`toast()` 调用处已统一 catch 展示 `error.message`，改动后错误文案自动生效；204 无响应体逻辑不变。

### 1.6 🟡 SPA 兜底吞掉未知 /api 路由（R11）

- **问题**：`app.get('/{*path}', ...)` 兜底对所有路径返回 `index.html`，导致**不存在的 API 路径**（如 `/api/orders/xxx`）也返回 200 + HTML；前端 `await response.json()` 解析失败抛 `SyntaxError`，掩盖真实问题。
- **位置**：`server/index.js` L295
- **建议方案**：
  ```js
  app.get('/{*path}', (request, response, next) => {
    if (request.path.startsWith('/api')) { response.status(404).json({ error: '接口不存在' }); return }
    response.sendFile('index.html', { root: 'dist' })
  })
  ```
- **注意事项**：`uploads` 下的静态资源由 `express.static('dist')` 之后是否另有静态挂载需确认（若 uploads 走独立静态目录，勿被兜底拦截）。

### 1.7 🟢 前端空值守卫与渲染错误边界（R13 / R14）

- **R13 空值守卫**：`loadLatestOrder` 已对 `order.items` 做 `Array.isArray` 校验，但其余接口响应（如 `result?.id`、`messages.find`）依赖可选链，建议对所有 `render()` 依赖的 state 字段做兜底默认值（`state.picks = []` 等），避免脏数据白屏。
- **R14 渲染错误边界**：`render()` 基于 innerHTML 全量重绘，若任一视图函数抛错 → 整页空白且无提示。建议：
  ```js
  function render() {
    try { app.innerHTML = viewTemplate() }
    catch (error) { console.error(error); app.innerHTML = `<div class="error-card">页面渲染失败，请刷新重试</div>` }
  }
  ```
- **位置**：`main.js` `render()` 及 `loadLatestOrder` / `loadOrderHistory` / `loadMessages` 的响应处理。

---

## 二、P2 —— 数据库冗余清理（需手动 SQL，建议与下次发版一起执行）

> 依据：`sql/hewei_order.sql` 与 `server/db.js` 当前建表/迁移逻辑对比 + 全代码 SELECT/UPDATE 扫描。
> **原则**：仅删除「无任何代码读写」的列/索引；删除前先用下述清单核对当前 `server/` 代码已无引用。

### 2.1 冗余字段清单

| 表 | 字段 | 现状 | 建议 |
|---|---|---|---|
| `orders` | `table_number` | 永远写 `''`，无读取 | 🗑 DROP |
| `orders` | `guest_count` | 永远写 `1`，无读取（含 CHECK 约束） | 🗑 DROP |
| `orders` | `meal_period` | 写死 `'午餐'`，无业务读取（餐次功能已下线） | 🗑 DROP |
| `order_items` | `created_at` | 无任何 SELECT 使用 | 🗑 DROP |
| `favorites` | `created_at` | 无任何读取 | 🗑 DROP |
| `messages` | `meal_period` | 无读取（餐次功能已下线） | 🗑 DROP |
| `dishes` | `meal_periods` | 无 SELECT 使用（餐次功能已下线，前端已删过滤逻辑） | 🗑 DROP |
| `dishes` | `sales` | 从不自增，无读取 | 🗑 DROP |
| `dishes` | `is_custom` | 仅在 createDish 写入 `true`，无读取 | 🗑 DROP |

> ⚠️ **暂不动的字段**：`dishes.image_url`（核心）、`dishes.category`（按分类过滤在用）、`orders.status`（枚举核心）、`messages.updated_at`（前端显示更新时间）、`app_metadata`（种子版本）。

### 2.2 冗余索引清单

| 索引 | 位置 | 现状 | 建议 |
|---|---|---|---|
| `idx_dishes_name` | `dishes.name` | 菜品搜索在**前端**内存过滤，SQL 从不 `WHERE name LIKE` | 🗑 DROP |
| `idx_dishes_category` | `dishes.category` | 查询按 id / 全量拉取，无按分类 SQL 过滤 | 🗑 DROP |
| `idx_messages_period` | `messages.meal_period` | 餐次功能下线，随列一起删 | 🗑 DROP |
| `idx_messages_created_at` | `messages.created_at` | `ORDER BY id` 已覆盖（id 递增），无按时间 SQL 查询 | 🗑 DROP（可选） |
| `idx_orders_status_period` | `orders(status, meal_period)` | 餐次功能下线，复合索引失去意义 | 🗑 DROP |
| `uk_messages_order` | `messages.order_id` | ✅ **已加（P0）**，保留 | 保留 |

### 2.3 缺失项（已补/待补）

- ✅ `messages.order_id` 唯一索引 `uk_messages_order` —— **P0 已通过 `initializeDatabase` 幂等迁移添加**。
- ❌ `messages.order_id` 外键 —— 建议补（当前无 FK，`deleteOrder` 时留言成孤儿；不过当前 `orders` 除 completed 外都被删除，留言随 `order_id` 悬空，需同步清理策略）。
- ❌ `sql/hewei_order.sql` 与代码不一致 —— 该文件是 Navicat 早期导出，缺少 `uk_messages_order`，且含代码不再创建的 CHECK 约束；建议实施 P2 后**重新导出**。

### 2.4 存量数据一次性清理

P0 起 `createOrder` / `clearCurrentOrder` 已改为删除非 completed 订单，但**历史遗留的 cancelled/draft 死单**仍在库中，建议执行：

```sql
-- 清理所有未完成的历史订单（cancelled / draft / confirmed 之外的残留）
DELETE FROM orders WHERE status != 'completed';
-- 清理由此产生的孤儿 order_items（理论上已随订单级联删除，双保险）
DELETE oi FROM order_items oi LEFT JOIN orders o ON o.id = oi.order_id WHERE o.id IS NULL;
```

---

## 三、建议执行顺序与部署

| 阶段 | 内容 | 验证方式 |
|---|---|---|
| P1-A | R2 鉴权/限流、R7 菜品校验、R11 API 404 | `get_errors` + 手动 curl 各接口 |
| P1-B | R6 413、R8 图片 GC、R9 错误体、R13/R14 守卫 | `get_errors` + 浏览器冒烟 |
| P2 | SQL 清理（字段/索引/存量数据）→ 重导 `hewei_order.sql` | 先备份再执行；`SHOW COLUMNS` 核对 |

**部署提醒**（沿用项目既有流程，本人不运行构建/测试命令）：

```bash
# 后端（改动 server/ 后）
scp -r server/* ubuntu@118.89.135.164:/opt/hewei-order/server/
ssh ubuntu@118.89.135.164 'cd /opt/hewei-order && pm2 restart hewei-api'

# 前端（改动 main.js 后）
npm run build
npm run android:sync
```

> ⚠️ P2 的 DROP 语句在远端数据库执行前，务必先在本地/测试库验证，并 `mysqldump` 备份。

---

## 附：P0 变更回顾（2026-08-12 已完成）

1. **订单号服务端生成**：`createOrder` 不再接收 `orderNumber`，内部生成 `ORD${Date.now()}${uuid前6位大写}`；`orderSchema` 移除 `orderNumber`；前端 `syncOrder` 改用 `result.orderNumber`。
2. **订单表膨胀修复**：`createOrder` / `clearCurrentOrder` 改为 `DELETE FROM orders WHERE status != 'completed'`（cancelled/draft 死单不再累积）。
3. **updateDish 误报 404 修复**：先 `SELECT id` 判存在，不再依赖 `affectedRows`（内容未变更保存不再误 404）。
4. **每单一留言（R5）**：`messages` 增加唯一索引 `uk_messages_order`（迁移时自动去重，优先保留非空、最新一条）；`updateMessage` 事务内删除目标订单旧留言；`createMessage` 撞索引时返回已存在留言；前端本地 `messages` 同步剔除同订单旧留言。

**受影响的测试**（已同步更新，运行时需确认通过）：
- `tests/order-sync.test.js`、`tests/message-update.test.js`：`createOrder` 调用移除 `orderNumber`，断言改为服务端生成格式 `/^ORD/`。
- `tests/database-only.test.js`：`clearCurrentOrder` 断言改为 `DELETE FROM orders WHERE status != 'completed'`。
